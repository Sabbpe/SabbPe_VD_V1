# Sabbpe Payment Gateway - Backend

## Setup
1. Clone repository
2. Copy `.env.example` to `.env`
3. Fill in actual credentials in `.env`
4. Run `npm install`
5. Run `npm start`

## Environment Variables
See `.env.example` for required variables.

**Never commit the actual `.env` file!**
